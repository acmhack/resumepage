export interface IUserCard {
    name: string;
    resumelink: string;
    projectlink: string;
    grad: string;
    category: string;
    projectName: string;
    school: string;
    featured: boolean;
}